先别写代码。读 @README.md 和 @sample_testcases，你的上下文有限，
把分析结果直接写入以下两个文件（不要在对话里输出，节省 token）：

@spec.md：
① 硬约束清单（尤其反直觉的，全部列出，编号 C1、C2...）
② 每个接口的入参/出参/失败返回（分组列表）
③ 最容易翻车的点（编号 R1、R2...）

@ tasks.md：
④ 5-8 个子任务，每个 [ ] 开头，按依赖顺序排列
   格式：- [ ] T1: 任务描述（依赖：无 / T0）

写完只在对话里回复"已生成 spec.md 和 tasks.md，请 review"。
不确定的地方在文件里标"? 不确定"，禁止脑补。

@tasks.md @spec.md @README.md @main.py 现在只做task.md中的【T1,T2 T3 T4 T5 T6】。要求：
① 严格遵守 @spec.md 里的硬约束
② 写完把 @tasks.md 里 的 [ ] 改成 [x]
③ 自己用 requests/subprocess 跑一遍相关样例，打印实际返回 vs 预期
④ 不一致就告诉我差在哪，不要顺手改其他任务